class Solution {
    public int solution(int num1, int num2) {
        // result 변수는 선언과 동시에 바로 값이 할당됨
        // (0으로 초기화했다가 곧바로 덮어쓰는 불필요한 과정 생략 가능)
        int result = num1 / num2;

        return result;
    }
}